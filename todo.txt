# 1. Set your working application with the CLI

2. Simple task (generate an index scripting, use CLI to upload as an index)

3. More complicated task (take a snapshot, modify rules, revert to snapshot)
